This test processes incoming video stream and makes an object detection.

webhook_test_server.py - You can use this script to run a server that receives webhook POST requests.

run.sh – Use this script to test the trigger.