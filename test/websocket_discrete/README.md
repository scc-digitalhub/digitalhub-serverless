This test checks that the WebSocket trigger correctly handles discrete mode, where data is delivered in individual chunks — in the case of the test, as text messages.

run.sh — Run this script to test the trigger.