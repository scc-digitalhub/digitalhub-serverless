This test checks that the WebSocket trigger correctly handles incoming streaming data, in the case of the test — audio, which is then transcribed using the Whisper library.

An external streaming server is needed for the test.

run.sh – Run this script to test the trigger.